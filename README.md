# breast-cancer-classification
This project uses the Breast Cancer Wisconsin dataset to build a logistic regression model that classifies tumors as malignant or benign. It includes data preprocessing, feature selection, model training, and evaluation using accuracy, precision, recall, F1-score, and ROC-AUC metrics.
